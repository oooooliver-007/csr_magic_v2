# CSR Magic 前端服务规则

## 技术栈
- Vite 6 + React 19 + TypeScript 5（严格模式）
- TailwindCSS 4 + Lucide React（图标）+ Recharts（图表）
- Zustand（全局状态）+ React Query（服务端状态）
- React Hook Form + Zod（表单验证）
- Axios（HTTP 客户端）+ React Router 7（路由）

## 目录结构
```
src/
├── components/       # 可复用组件
├── pages/            # 页面组件（按路由组织）
│   ├── admin/        # 管理端页面
│   └── ...           # 员工端页面
├── services/         # API 封装（每模块一个文件）
├── types/            # TypeScript 类型定义
├── stores/           # Zustand store
├── hooks/            # 自定义 Hook
├── constants/        # 常量定义
└── App.tsx           # 根组件 + 路由配置
```

## 编码规范
- 组件使用函数式 + Hooks，禁止 class 组件
- Props 用 interface 定义，文件顶部声明
- 单个组件不超过 200 行，大组件拆分
- 自定义 Hook 以 `use` 开头，放在 `hooks/`
- 所有 API 通过 `services/` 层封装，禁止组件内直接调用 fetch/axios
- 样式用 TailwindCSS utility classes，禁止内联 style
- 响应式断点：`md:`（768px）为主断点
- 禁止使用 `any` 类型
- API baseURL 使用环境变量 `VITE_API_BASE_URL`

## 文件命名
- 组件文件：PascalCase.tsx（`ActivityCard.tsx`）
- 工具/服务文件：camelCase.ts（`activityApi.ts`）
- 类型文件：camelCase.ts（`activity.ts`）
- 测试文件：与源文件同目录，`*.test.ts` / `*.test.tsx`

## UI 设计规范
- 设计令牌参考 `docs/shared/ui-design-tokens.md`（颜色、圆角、阴影、排版）
- 页面 UI 必须遵循 `UI_UX_prototype/` 中对应原型组件的交互和视觉设计
- 主色 `#2EB87A`、强调色 `#FFB347`、背景 `#F7FAF8`、文字 `#1A2E22`
- 卡片 `rounded-2xl shadow-sm`，按钮/输入框 `rounded-xl`
- 员工端：顶部导航 + 内容区；管理端：左侧边栏 + 右侧内容区

## 端口
- 开发服务器：3000
