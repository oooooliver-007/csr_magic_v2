# CSR Magic AI 服务规则

## 技术栈
- Python 3.11+ + FastAPI + Pydantic v2
- httpx（异步 HTTP 客户端）
- dashscope SDK（阿里云 DashScope API）
- Pillow（图像处理/合成）
- AI 对话：Qwen（通义千问）
- AI 图像：Wan 2.7 Image Pro（通义万相）

## 目录结构
```
app/
├── api/              # 路由处理（poster.py, chat.py）
├── agents/           # AI Agent 逻辑
│   ├── poster_agent.py    # 海报生成主流程
│   ├── chat_agent.py      # 对话报名 Agent
│   └── image_gen.py       # 通义万相 API 封装
├── utils/            # 工具函数
│   ├── image_compose.py   # Pillow 合成海报
│   ├── prompt_builder.py  # Prompt 模板构建
│   ├── field_parser.py    # 对话字段提取
│   └── storage.py         # 文件存储
├── prompts/          # Prompt 模板文件
├── models.py         # Pydantic 请求/响应模型
└── config.py         # 配置（环境变量）
main.py               # FastAPI 应用入口
```

## 编码规范
- 所有 IO 操作使用 async/await
- HTTP 调用使用 httpx.AsyncClient，禁止 requests 同步调用
- Pydantic v2 模型做请求/响应验证
- Prompt 模板化，支持变量替换，禁止硬编码 prompt 文本
- DashScope API Key 必须通过环境变量 `DASHSCOPE_API_KEY` 获取
- 日志使用 Python logging 模块，禁止 print
- 测试文件放在 `tests/`，命名 `test_*.py`
- DashScope API 调用在测试中必须 Mock

## API 规范
- 海报生成：`POST /poster/generate` + `GET /poster/{task_id}`
- 对话报名：`POST /chat/sessions` + `POST /chat/sessions/{id}/messages` + `GET /chat/sessions/{id}`
- 响应格式与后端一致：`{ code, message, data }`

## 端口
- HTTP 服务：8000
